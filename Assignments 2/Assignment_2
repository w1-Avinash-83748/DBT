							Assignment No:2
1) Which field of the Customers table is the
primary key?
-> CNUM


2) What is the 4th column of the Customers table?
->RATING

3) What is another word for row? For column?
->For Row : Record, Tuple, Entity, Opportunity
  For Column : Attribute, Method, Key, Field, 
	

4) Why isn’t it possible to see the first five
rows of a table?
->SQL tables are unordered sets, so there is no such thing as the first five rows
 or last row unless a column explicitly defines the ordering
